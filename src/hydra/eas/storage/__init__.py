"""EAS storage mappings and bootstrap — populated in later tasks.

Holds Elasticsearch index mappings (``hydra-screenshots``, ``hydra-cves``)
and shared read-side repositories (Design §2.4, §4.11).
"""
